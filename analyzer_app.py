# Compatibility entrypoint for Streamlit deployments that were originally
# configured to launch analyzer_app.py. Importing app runs the combined
# Momentum Scanner + Stock Analyzer workspace.
import app
